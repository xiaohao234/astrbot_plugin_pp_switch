{
  "trigger_words": {
    "type": "list",
    "description": "触发词列表（无需@机器人）。消息完全等于触发词、或为「触发词 + 空格 + 序号」时生效。修改后需重启插件。",
    "default": ["pp", "人格切换"]
  },
  "admin_only": {
    "type": "bool",
    "description": "仅管理员可切换人格（查看列表不受此限制）",
    "default": false
  },
  "intro_max_len": {
    "type": "int",
    "description": "图片中每个人格介绍的最大字符数（介绍取自人格提示词的第一条有效内容）",
    "default": 60
  },
  "image_width": {
    "type": "int",
    "description": "人格列表图片的宽度（像素，高度自适应）",
    "default": 900
  },
  "image_scale": {
    "type": "int",
    "description": "图片超采样倍数（1~3，越大越清晰、生成越慢）",
    "default": 2
  },
  "font_path": {
    "type": "string",
    "description": "自定义中文字体文件路径（留空则自动探测系统字体；服务器建议安装 fonts-noto-cjk）",
    "default": ""
  },
  "show_current": {
    "type": "bool",
    "description": "在列表图片中标记当前会话正在使用的人格",
    "default": true
  },
  "help_lines": {
    "type": "list",
    "description": "图片底部的帮助说明（每条一行；留空则使用默认文案）",
    "default": [
      "发送 pp <序号> 切换到对应人格，例如：pp 2",
      "发送 pp 或 pp 列表 查看人格列表",
      "发送 pp 当前 查看当前人格"
    ]
  },
  "base_persona": {
    "type": "string",
    "description": "底层人格名称（选填）：先在 AstrBot「人格角色设定」创建一个占位人格（提示词写「请等待插件注入人格」之类），并把它设为 AstrBot 默认人格，再把名称填到这里。配置后该人格不在 pp 列表中显示、不参与编号；发送 pp 复位 可随时切回它",
    "default": ""
  },
  "require_at": {
    "type": "bool",
    "description": "群聊必须 @ 机器人（或使用唤醒前缀/引用唤醒）才能触发 pp，防止多个机器人同时响应同一条消息；私聊与 WebChat 不受影响。触发后由插件直接回复并停止传播，不会把 pp 当作聊天内容发给 LLM",
    "default": false
  },
  "persona_prefix": {
    "type": "bool",
    "description": "在 LLM 回复前附加 [当前人格名] 前缀，如 [音游大神]你好呀。仅对 LLM 回复生效；未选择人格或底层人格待机（只回复……）时不加；在分段回复之前处理，不影响分段功能",
    "default": false
  },
  "hidden_personas": {
    "type": "list",
    "description": "不在 pp 列表图片中显示的人格名称（由 pp 隐藏/pp 显示 指令维护，也可在此手动编辑）。注意：隐藏仅影响图片显示，序号保持不变、仍可通过 pp 序号 切换",
    "default": []
  },
  "hide_managers": {
    "type": "list",
    "description": "允许使用 pp 隐藏 / pp 显示 指令的 QQ 号列表（一行一个）。留空则所有人都无法使用该指令；AstrBot 管理员也不会自动获得该权限",
    "default": []
  },
  "switch_handoff": {
    "type": "bool",
    "description": "切换后在接下来的几次 LLM 请求中临时附加人格强化提示，抵消旧记忆里的旧人格语气、避免人设残留（只影响请求，不写入、不修改任何聊天记忆）",
    "default": true
  },
  "switch_clear_history": {
    "type": "bool",
    "description": "切换人格后清空当前会话的 LLM 上下文（对话记忆从头开始）。只清对话记忆，不会删除聊天记录——WebUI 的聊天记录仍可查看（保留条数由 AstrBot 配置 provider_ltm_settings.group_message_history_max_cnt 决定，默认 700，可自行改为 200）",
    "default": false
  }
}
